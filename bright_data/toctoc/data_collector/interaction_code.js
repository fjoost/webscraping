country('cl');
navigate(input.url, {timeout: 45000}); // the number of milliseconds to wait for. Default is 30000 ms
let data = parse();
// andes tab > todos los buttons
let gps = data.gps;

console.log(gps);
collect(parse());